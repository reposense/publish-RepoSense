language: java

dist: xenial

python: "3.x"

install: pip install requests

script: ./run.sh

deploy:
  skip_cleanup: true
  provider: script
  script: . ./configs/travis.sh && . ./configs/deploy.sh
  on:
    branch: master
